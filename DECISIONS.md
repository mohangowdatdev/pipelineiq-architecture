# PipelineIQ — Decision Log

Running log of every architectural decision made.
Add a new row immediately when any choice is made during a session.
Never remove rows. Superseded decisions get a "superseded by" note.

| # | Date | Decision | Choice | Rationale |
|---|------|----------|--------|-----------|
| 1 | Apr 2025 | Vector store | pgvector on PostgreSQL Flexible Server | Single service for relational control plane AND vector search. No separate vector DB needed. Cheaper, simpler, one connection string, one cost line. Featured architectural pattern — highlight in all demos. |
| 2 | Apr 2025 | AI model | Claude Sonnet 4.6 via Microsoft Foundry | Better structured JSON output than GPT-4o for IaC code understanding and multi-constraint prompts. Billed through Azure subscription. Same token pricing as Azure OpenAI. |
| 3 | Apr 2025 | SQL serving layer | Databricks SQL Warehouse only | Native Delta Lake awareness, JDBC/ODBC connectable from VS Code and Power BI. Synapse Analytics dropped entirely — not needed, and moving toward retirement by Microsoft. |
| 4 | Apr 2025 | Databricks tier | Premium | Required for Unity Catalog, audit logs, RBAC, and diagnostic settings. Standard tier retired April 2026. Premium is effectively the only valid choice for new workspaces. |
| 5 | Apr 2025 | Dataset | Velora synthetic generator only | Full control over failure injection, commercially credible narrative, no external API dependencies. NYC Taxi and Chicago Taxi dropped — weak portfolio narrative, no order lifecycle, no SCD events. |
| 6 | Apr 2025 | Source DB | Azure SQL Database serverless tier | Auto-pause when idle, native ADF linked service, managed identity access, no VM overhead. Managed Instance rejected — 20x the cost, no features needed for this use case. |
| 7 | Apr 2025 | Modular architecture | Azure-native, swap-ready by documentation | No premature abstraction. Each module README documents AWS and GCP equivalent services. Swap-readiness is documented, not engineered. New client = new clients/ folder + fill variables + terraform apply. |
| 8 | Apr 2025 | Synapse Analytics | Dropped entirely | Databricks SQL Warehouse serves all SQL needs natively. Synapse is on a slow retirement path from Microsoft. No reason to include it. |
| 9 | Apr 2025 | Alert delivery | Azure Functions triggering Slack webhook | Functions already in stack for control plane. Logic Apps rejected — unnecessary additional service. |
| 10 | Apr 2025 | Region | Central India | Lowest latency for India-based development. All cost estimates use Central India rates. |
| 11 | Apr 2025 | Currency | 1 USD = Rs. 95 | Current approximate rate at time of planning. All cost estimates use this rate. |
| 12 | Apr 2025 | IaC tooling | Terraform for infrastructure + Bicep for ADF-specific resources | Terraform has better Azure provider coverage for general resources. Bicep is more natural for ADF pipeline JSON, linked service definitions, and dataset configs which are Azure-specific by nature. |
| 13 | Apr 2025 | PostgreSQL tier | B2s (2 vCore, 4GB RAM) | Sufficient for control plane operations + pgvector similarity search at this volume. Burstable tier handles the occasional spike. Cost controlled by stopping server nights/weekends. |
| 14 | Apr 2025 | Watermark strategy | Advance watermark only on confirmed pipeline success | Failure leaves watermark unchanged so next run retries the same window. No data loss on failure. No duplicate processing on retry because MERGE is idempotent. |
| 15 | Apr 2025 | SCD choices | Type 2 on customer (segment/city), product (list_price), sales_rep (territory). Type 1 on territory, store. Type 0 on static lookups. | Type 2 where historical accuracy matters for analysis (did this customer buy as Individual or Business?). Type 1 where the old value has no analytical use. Type 0 for truly static reference data. |
| 16 | Apr 2025 | Project file structure | CLAUDE.md (anchor) + PLANNING.md + PROGRESS.md + SCHEMA.md + DECISIONS.md + docs/ folder | CLAUDE.md convention is standard for Claude Code agentic sessions. docs/ written at end of each phase, not speculatively before. One objective per coding session. |
| 17 | Apr 2025 | Failure injector approach | Flag in generator, not separate tool | Generator already knows the schema and data relationships. A flag that produces a controlled bad batch is simpler than a separate injection tool. Deterministic with fixed seed for reproducible demos. |
| 18 | Apr 2025 | AI model (revised) | Azure OpenAI GPT-4o | Foundry provisioning blocked on sponsorship subscription credits. Switching to Azure OpenAI Service which works with subscription credits. Swap back to Claude Sonnet 4.6 via Foundry when on a paid subscription. Decision is temporary — prompt structure and JSON response schema remain identical so the swap back is a one-line config change. |

| 19 | Apr 2026 | Catalogue entity ID generation | UUID5 (deterministic, hash-based) | UUID4 is random — re-seeding the catalogue after a table drop produces different IDs and breaks all FK references in order_lines and inventory_snapshot. UUID5 from a fixed namespace produces the same UUID for the same logical entity every time, making the seeder idempotent. |
| 20 | Apr 2026 | Inventory snapshot transaction scope | Separate transaction from main data batch | The snapshot is 189,000 rows (4,200 products × 45 stores). Including it in the main transaction increases lock hold time and timeout risk on Azure SQL serverless. It's also idempotent by snapshot_date — failure is safe to retry. Main batch (customers + orders + status + dimension changes) must be atomic together; snapshot does not need to be. |
| 21 | Apr 2026 | Dependency violation failure scenario | Control flag in velora_oms.control_flags table | The dependency_violation scenario needs a mechanism for ADF to read a signal that triggers out-of-order execution. A lightweight control_flags table in Azure SQL is the simplest approach — ADF Web Activity can read it. Not extracted by ADF watermark, not in Gold model, purely operational. |
| 22 | Apr 2026 | Generator module separation | One module per domain (customers, orders, status, dimensions) | Each module owns one domain of write operations. Customers.py generates INSERTs and UPDATEs for CRM tables only. Orders.py writes to OMS only. This separation means a bug in one module cannot corrupt another domain's data. It also makes each module independently testable without running the full generator. |
| 23 | Apr 2026 | Failure injector scope | Mutates in-memory batch before DB write, not after | The injector operates on pandas DataFrames before any SQL transaction opens. This means: (1) the failure is guaranteed to be written or not written as a unit, (2) no partial-injection state can exist in the DB, (3) the injector is independently testable without a DB connection. |
| 24 | Apr 2026 | Session journal format | Structured session log appended to PROGRESS.md at end of every session | A free-form "Updated: date" footer is too weak a record — after many sessions it becomes impossible to reconstruct what happened when. A structured log (Objective / Built / Worked / Broke / Uncertainty / Next / Summary) gives the next session (human or Claude) a precise pickup point. Lives in PROGRESS.md under ## Session Log, not in a separate file, so it's always read alongside phase status. CLAUDE.md Session Discipline Rules updated to enforce this. |
| 25 | Apr 2026 | Azure OpenAI region | South India (split from primary Central India region) | Azure OpenAI is not offered in Central India. Closest supported India region is South India. Rather than move the entire stack to South India for a single service, keep all hot-path / data / pipeline resources in Central India (per #10) and host Azure OpenAI in South India. The only cross-region call is FastAPI → OpenAI for RCA, which is async and latency-insensitive. All other components co-located in Central India. |
| 26 | Apr 2026 | Build sequence tracking | `docs/build_order.md` as single source of truth for provisioning order and status | PROGRESS.md tracks phase-level status and per-session activity. DECISIONS.md tracks the "why" of choices. Neither captures the granular dependency-ordered provisioning sequence (Tier 1 state backend → Tier 2 core → Tier 3 source → …). build_order.md fills that gap: every resource listed, in dependency order, with status column. Updated as items land. Avoids the failure mode of "wait, did we already create the Key Vault?" across sessions. |
| 27 | Apr 2026 | Terraform azurerm provider version | Pinned to `~> 4.0` (v4.69.0 installed, pinned by lock file) | Spec in CLAUDE.md says `azurerm >= 3.90`. v4.x is current stable and satisfies that. v4 gives us modern attribute names (`rbac_authorization_enabled` vs deprecated `enable_rbac_authorization`), better block syntax for `features {}`, and newer resource types we'll need in later tiers. `.terraform.lock.hcl` pins the exact version so all developers get the same provider binary. |
| 28 | Apr 2026 | Resource group topology | Single RG `pipelineiq-rg-dev` holds both state backend and workload resources, referenced via `data "azurerm_resource_group"` | Simplest dev model. Terraform-managed RG + state-in-that-RG is a chicken/egg problem (can't destroy RG while state lives in it). The RG is created manually by `bootstrap_state.sh`, and every client composition references it as a `data` source rather than a `resource`. In prod we'd split into a separate state RG (possibly in a separate subscription) but that's not needed for dev/demo. |
| 29 | Apr 2026 | IaC module structure | Submodule pattern: `core/{service}/` each with `main.tf` + `variables.tf` + `outputs.tf`, composed by `clients/{client}/main.tf` | Multi-tenant future (clients/velora/, future clients/*) needs reusable per-service primitives with well-defined inputs/outputs. Flat module files in a single directory don't scale past one client and make variable scoping messy. Submodules are the industry standard for Terraform monorepos with multiple compositions (e.g., terraform-aws-modules, Gruntwork's patterns). |
| 30 | Apr 2026 | RBAC grant inside each module | Each core module optionally grants the running principal the data-plane role (Key Vault Secrets Officer, Storage Blob Data Owner, etc.) at creation time, controlled by a `grant_current_user_*` bool | Without this, the first `terraform apply` creates resources but fails immediately after when the same apply tries to create sub-resources (filesystems, secrets) because the running principal has no data-plane permission yet. Inline RBAC grant closes the bootstrap gap. The bool lets prod compositions turn it off and rely on pre-existing service-principal access. |
| 31 | Apr 2026 | Managed identity module deferred | Build order item 2.6 (`core/identity/` module) deferred | Each downstream resource (ADF, Databricks, Functions, Container Apps) gets a system-assigned managed identity at creation time. User-assigned identities are only needed when multiple resources share one identity, which is not yet a requirement. Can introduce a `core/identity/` module later when a cross-resource identity need appears, without breaking existing modules. |
| 32 | Apr 2026 | Both repos public on GitHub under `mohangowdat-sail/` | Public: `pipelineiq-architecture` and `pipelineiq-iac` | Portfolio project — public repos show the full architecture and decision trail end-to-end. Secrets remain gitignored (`.env`, `.env.*.local`, `*.tfvars`, state files, `*.tfplan`, Key Vault references). `terraform.tfvars.example` currently contains the real Azure Sponsorship subscription ID and Sail tenant UUID: these are identifiers, not credentials — they cannot be used to authenticate — but they do publicly associate the tenant with this repo and narrow an attacker's targeting surface. **Retrospection trigger:** before going beyond Phase 3 or if PipelineIQ onboards any second tenant, review whether to scrub the UUIDs from `terraform.tfvars.example` and replace with placeholders. Until then, leave as-is for demonstrability. |
| 33 | Apr 2026 | Portal live-demo backend architecture | React SPA on Vercel + Node.js Serverless Functions in `frontend/api/` (`generate-incident`, `chat-incident`, `send-slack-alert`). FastAPI backend (`backend/`) deferred — local-dev only, not deployed anywhere. | The live demo fakes incidents via Azure OpenAI (no DB writes) and posts Slack alerts — both are perfectly served by Vercel Functions with zero additional infrastructure, no managed database, no hosted-auth surface. Keeps the live demo free-tier-friendly and the secret footprint minimal (just `AZURE_OPENAI_API_KEY` + `SLACK_WEBHOOK_URL` on Vercel). FastAPI (auth + CRUD + Alembic + SQLAlchemy + seed data) is kept around for scenarios that need a real persistent backend and for local full-stack development; Phase 6's production dashboard inside Architecture is where full persistence lands. Portal is frozen pending Phase 6 supersession. |
| 34 | Apr 2026 | Azure AI resource for Portal — actual location | Resource `pipeline-iq-resource`, subscription `Microsoft Azure Sponsorship`, resource group `rg-pipelineiq`, region `centralus`, kind `AIServices` (not classic `OpenAI`). Deployment name `gpt-5.4-mini`. | Load-bearing fact — the resource does **not** live on the `SSE BI Subscription` (the client subscription for PipelineIQ-Architecture work); `az account list` must be `--subscription "Microsoft Azure Sponsorship"` for any Portal debugging. The resource name appears under BOTH `https://pipeline-iq-resource.openai.azure.com/openai/v1/` (classic OpenAI compat URL, used by Portal JS today) AND `https://pipeline-iq-resource.services.ai.azure.com/api/projects/pipeline-iq/openai/v1/` (Foundry project URL shown in the deployment page) — Azure routes both to the same backend, so either `baseURL` is valid with the OpenAI SDK. Auth: `Authorization: Bearer <key1>` on either URL. **Canonical retrieve command:** `az cognitiveservices account keys list --subscription "Microsoft Azure Sponsorship" -n pipeline-iq-resource -g rg-pipelineiq --query key1 -o tsv`. Future sessions debugging 401s: verify the stored Vercel env var value matches this fresh `az`-retrieved key before touching code. |
| 35 | Apr 2026 | Subscription-level Owner for the running identity | `mohan.gowda@SailAnalyticsAP.onmicrosoft.com` elevated from Contributor → Owner on the `Microsoft Azure Sponsorship` subscription (object id `44ca08a6-faf0-4267-bae5-7ed148a082aa`). Alternative offered: UAA at RG scope. | The `grant_current_user_*` inline RBAC pattern in every core module (DECISIONS #30) requires `Microsoft.Authorization/roleAssignments/write` at the resource scope. The user started with only Contributor, so the first apply created Key Vault + LA + ADLS but failed on the 2 role assignments + 5 filesystems. Sole existing Owner is `admin@SailAnalyticsAP.onmicrosoft.com`. User self-elevated via Portal IAM. This unblocks every current and future tier that creates inline role assignments (Postgres AAD admin, SQL AAD admin, future Databricks UC, etc.), not just Tier 2. Least-privilege alternative recorded: RG-scoped UAA on top of existing Contributor — strictly sufficient since DECISIONS #28 mandates single-RG topology. |
| 36 | Apr 2026 | PostgreSQL + Azure SQL auth design | Dual-auth everywhere: (1) `random_password` resource generates 24-char admin password, stored in Key Vault (`postgres-admin-password`, `sql-admin-password`) + full connection string secret; (2) running AAD principal is made admin on each server. Preferred login path is AAD; password exists only for bootstrap + break-glass. | Password-only is rigid (rotation needs coordinated Terraform + consumer update); AAD-only blocks psql/sqlcmd in emergencies when AAD isn't reachable. Dual-auth gives both: day-to-day apps use AAD (via managed identity), humans use AAD + az login, password sits in Key Vault as break-glass. `random_password` with `override_special = "!#%*_-="` avoids characters that some Azure APIs reject in admin passwords. |
| 37 | Apr 2026 | Azure Postgres Flex `azure.extensions` parameter is case-sensitive (lowercase) | Allowlist values must be lowercase exactly as the API returns them: `vector`, `pg_trgm`, `uuid-ossp` (note hyphen, not underscore). Uppercase forms `VECTOR`, `PG_TRGM`, `UUID_OSSP` are **rejected** with `ServerParameterToCMSUnAllowedParameterValue`. | Caught during Tier 3 apply — 17 of 18 resources landed, only the `azure.extensions` config failed. Fixed in `core/postgres/variables.tf` default. Azure docs and most examples show uppercase, which is misleading; the enum is actually defined in lowercase internally. Also note: allowlisting the extension at server level ≠ creating it — bootstrap_postgres.sql still runs `CREATE EXTENSION IF NOT EXISTS vector;`. |
| 38 | Apr 2026 | Core module submodule expansion — 4 new modules | Added `core/postgres/`, `core/databricks/`, `core/openai/`, `source_connectors/azure_sql/`. Each follows the DECISIONS #29 pattern (`main.tf` + `variables.tf` + `outputs.tf` + `versions.tf`) with inline `grant_current_user_*` RBAC grants where applicable. | Extends the Tier 2 core (keyvault, log_analytics, adls) to cover the data plane. Each module is client-agnostic — composition happens in `clients/velora/main.tf`. `core/databricks/` is deliberately minimal (workspace only); Unity Catalog metastore + external locations deferred because they require the `databricks` provider with account-level credentials, which belongs in a separate apply stage post-workspace-creation. |
| 39 | Apr 2026 | Key Vault secrets composition lives in client, not in modules | `clients/velora/main.tf` creates `azurerm_key_vault_secret` resources for: `postgres-admin-password`, `postgres-connection-string`, `sql-admin-password`, `sql-connection-string`, `openai-api-key`, `openai-endpoint`. | Modules output sensitive values (admin_password, primary_access_key); the client composition glues them into Key Vault. This keeps modules focused on one Azure service and makes the secret-naming convention a client-level concern. When a second client (non-Velora) is added, its module calls stay identical but its Key Vault secret names can differ. |
| 40 | Apr 2026 | `pyodbc.Cursor.fast_executemany = True` is load-bearing for all `executemany` in the generator (and any future pyodbc-based code) | Every cursor created against Azure SQL must set `cursor.fast_executemany = True` before any `executemany` call. Applied at 3 sites: `generator/catalogue.py::_bulk_insert`, `generator/main.py::run` (main transaction cursor), `generator/main.py::_write_inventory` (inventory snapshot cursor). | Without this flag, pyodbc sends each row as a separate SQL statement (one TDS packet per row). Over laptop → centralindia network (~200ms RTT), 4,200 product inserts exceed the 30s connection timeout and hit `[08S01] TCP Provider: Error code 0x274C (10060)` / `Communication link failure`. With the flag, pyodbc binds parameters on the client side and sends one batched TDS request — 189K inventory rows went from unrecoverable timeout to ~5 minutes. Also note: `fast_executemany` is a **cursor-level** attribute (not connection-level), so it must be set on each new cursor. Any future pyodbc code against Azure SQL in this project must set it. |
| 41 | Apr 2026 | Generator RNG seed is **date-derived**, not constant | `main.py::run` computes `effective_seed = seed + run_date.toordinal()` and uses that for both `np.random.default_rng(...)` and `Faker.seed(...)`. The CLI `--seed` arg is still honored as the base. | With a constant `seed=42` across all dates, every day's run consumed the RNG stream in the same order and drew identical customer/address/order UUIDs — fine for the first day, but day 2+ collided on `PK_customers` (`velora_crm.customers`). Caught on Session 4 task 1 when we tried to seed day 16 after day 15 had landed: `IntegrityError 23000 duplicate key value is (00000000-0000-0000-53c8-ffe06310fde6)` — 15-for-15 collision between the two days' "new" customer batches. Session 3's verification only covered a single date, so the latent bug never surfaced. Date-derived seeding gives each date its own deterministic stream: same date → same output (reproducibility preserved), different dates → non-overlapping UUID space. Failure-injection determinism per date is also preserved. Catalogue seeding is unaffected because catalogue IDs are UUID5 (DECISIONS #19), independent of the RNG. |
| 42 | Apr 2026 | Azure SQL serverless wake-up requires `Connection Timeout ≥ 90s` in pyodbc | `generator/config.py::get_connection_string` sets `Connection Timeout=90`. Any other pyodbc connection string against `velora_oms` must use the same. | Azure SQL Database serverless tier auto-pauses after its configured idle window (60 min on `velora_oms`). The first connection attempt after pause triggers a wake-up that takes 30–60 seconds to complete. pyodbc's default 30s connection timeout fires before the DB is resumed, yielding `[HYT00] Login timeout expired (0)` — which is a **timeout**, not a **Login failed** error. Observed on Session 4 task 1: `resumedDate = 2026-04-22T10:38:44Z` landed ~54s after connection initiated, i.e. well after the 30s timeout tripped. Bumped to 90s as a reasonable upper bound for serverless cold start. `az sql db show --query status` confirms whether the DB is paused before diagnosing other causes. |
| 43 | Apr 2026 | Failure injection is an ADF/landing-layer concern, not a generator concern | `generator/failure_injector.py` will continue to exist but is **not invoked** for runs that target Azure SQL as the sink. Future ADF pipelines (Tier 6) or a dedicated `scripts/inject_failure_to_landing.py` will mutate Parquet files in `landing/{entity}/date=YYYY-MM-DD/` to produce the 6 failure fixtures. The generator's `--failure` CLI flag remains for cases where the generator writes directly to Parquet (e.g. local unit testing), but its effect against Azure SQL is documented as "no-op or silent column drop" for schema_drift, and similarly inappropriate for the other classes. | Session 4 task 1 attempted `python generator/main.py --date 2026-01-21 --failure schema_drift` against Azure SQL. The injector added a `promo_code` column to the orders DataFrame, but the `orders` INSERT uses an explicit column list and pandas silently dropped the extra column — day 21 orders committed as clean data. The injector's own docstring reveals why: *"Bronze: loads fine (schema-on-read Parquet — extra column is just null elsewhere). Silver: MERGE fails..."* — it was designed to mutate Parquet (schema-on-read), not relational rows. Real retail OLTP systems (which velora_oms simulates) don't spontaneously grow columns, insert NULL prices, or violate FKs at source — those anomalies appear during extraction and transformation. So the correct architectural home for failure injection is between the source DB and the landing lake, not inside the source DB itself. The generator stays a faithful source-system simulator; failures are introduced at the seam where real pipelines actually break. |
| 44 | Apr 2026 | Catalogue seed must include `velora_pim.stores` | `generator/catalogue.py::seed_to_db` now calls `_bulk_insert(cursor, "velora_pim.stores", [...], catalogue["stores"])` after `product_pricing` and before `sales_reps`. One-shot backfill of 45 stores ran during Session 4. | Original `seed_to_db` bulk-inserted categories, products, pricing, sales_reps, and territory_assignments — but **skipped** `stores`. `_build_stores()` built the DataFrame, `build_catalogue()` returned it in the dict, but the write-to-DB step never referenced it. Orders and inventory_snapshot both write `store_id` values sourced from `config.STORES` (in-memory Python list); with no FK constraint enforcing the match, Azure SQL accepted these rows silently. Consequence: 770 STORE-channel orders and 1.32M inventory rows pointed at `store_id` values that didn't exist in `velora_pim.stores`. Bug invisible in Azure SQL but would have produced empty Silver/Gold `dim_store` joins. Caught via data-quality verification in Session 4 after task 1's seed completed. The `territories` table in `_build_territories()` is similarly dead code — there is no `velora_pim.territories` table in `bootstrap_sql.sql`; territories exist only implicitly as `territory_id` strings in `stores` and `territory_assignments`. Left unchanged; `_build_territories()` can be deleted in a future cleanup pass. |
| 45 | Apr 2026 | Unity Catalog rollout is two-stage; Bronze ships on `hive_metastore` first (**superseded by #46** — Account Admin promotion landed in Session 5, single-apply path now viable). | **Stage 1 (unblocked now):** `core/databricks_uc/` Terraform module contains access connector (managed identity for ADLS), Jobs Compute cluster policy, SQL Warehouse (2X-Small, auto-stop 10 min), and Key-Vault-backed secret scope. Bronze / Silver / Gold schemas live inside the workspace's default `hive_metastore` catalog at `abfss://{layer}@pipelineiqadlsdev.dfs.core.windows.net/...`. **Stage 2 (blocked):** metastore assignment + 5 external locations + 3 UC catalogs (`bronze` / `silver` / `gold`) land in a second apply after `mohan.gowda` becomes a Databricks Account Admin. Migration from Stage 1 to Stage 2 is a one-time `CREATE TABLE ... USING DELTA LOCATION` pass per table (~10 min). | Creating a Unity Catalog metastore is an **account-level** operation on Databricks, not a workspace-level one. The `databricks` Terraform provider requires the running principal to be a Databricks Account Admin (separate from AAD Global Admin, separate from workspace admin). `mohan.gowda` is a workspace admin ("global admin" in workspace-entitlement speak) but not an account admin — no "Manage Account" link appears in the workspace UI. Bootstrap path on Azure Databricks: a Sail AAD Global Administrator must log into `https://accounts.azuredatabricks.net` once, which activates the Databricks account for the tenant, then promote `mohan.gowda` to Account Admin. That's an external async dependency, so we stop-the-world until it resolves, OR we proceed on the well-understood `hive_metastore` path. Chose the latter — `hive_metastore` supports Delta Lake + ADLS external paths + the SQL Warehouse identically, and Bronze/Silver/Gold code is catalog-agnostic. UC lands as a migration, not a prerequisite. Paths A (block) and B (pure hive_metastore with no plan to migrate) both considered; Path C chosen to keep momentum without abandoning the UC architectural target. |
| 46 | May 2026 | UC metastore is **adopted** (data source) not **created** (resource). Two-stage rollout from #45 collapsed into a single Terraform apply. | `core/databricks_uc/main.tf` declares `data "databricks_metastore" "this"` referencing `metastore_id = "a2d5ffb1-1ac9-42ec-babb-80eacf4ba2fb"` (`metastore_azure_centralindia`, the system-created metastore). Catalogs (`bronze` / `silver` / `gold`), external locations, and storage credential are created normally; metastore + assignment **not** created. `clients/velora/main.tf` passes the metastore_id explicitly. | After `mohan.gowda` was promoted to Databricks Account Admin (DECISIONS #45 unblock path), Terraform tried to create `pipelineiq-dev-metastore`. Apply failed: `cannot create metastore: This account ... has reached the limit for metastores in region centralindia`. Databricks enforces **1 metastore per region per account**, and a system metastore had been auto-created when the admin first signed into `accounts.azuredatabricks.net`. The workspace was already auto-assigned (`metastore_assignment_status: AUTO_ASSIGNMENT_ENABLED`). Three paths considered: (a) destroy + re-create the system metastore — rejected, system-owned + would detach the workspace; (b) keep #45 two-stage with hive_metastore migration pending — rejected, more work for zero benefit; (c) **adopt the system metastore via data source and create everything else normally** — chosen. Single-apply, clean state, no migration debt. UC catalogs + external locations + storage credential are catalog/location-scoped (not metastore-scoped), so it doesn't matter who owns the metastore as long as one exists per region. Future tenants in other regions hit the same auto-create — same adoption pattern applies. The Stage 1 / Stage 2 split from #45 is now obsolete. |
| 47 | May 2026 | Landing-layer Parquet export uses two extraction modes (`by_date` + `full`) keyed off business-date columns, not audit columns. | `scripts/export_velora_to_landing.py::ENTITIES` manifest declares per-entity mode. `by_date` entities (orders → `order_date`, inventory_snapshot → `snapshot_date`) partition by an explicit business-date column; `full` entities dump the entire table once per run (order_lines, customers, customer_addresses, products, product_pricing, order_status_log, sales_reps, territory_assignments). Output paths: `landing/{entity}/date=YYYY-MM-DD/...` for by_date, `landing/{entity}/full/...` for full. | First-pass design used `updated_at` / `created_at` as watermark columns (matching SCHEMA.md's stated ADF watermark contract). Smoke test against `velora_oms` returned 0 rows for everything except `inventory_snapshot.snapshot_date`. Investigation revealed the generator left audit columns at the `GETUTCDATE()` default — they record seed time (2026-04-21/22), not the logical run date. Production ADF will still use `updated_at` for incremental CDC because by then the source system will be writing realistic audit timestamps; this scaffold script just needs *something* to partition on. Master tables (customers, products, sales_reps) have no logical run-date semantics anyway, so `full` mode is correct for them regardless. The script also adds resilience the generator's lessons: chunked `fetchmany(5000)`, fresh connection per read, retry-with-reconnect on transient `OperationalError` (5 attempts, exponential backoff). Caught one TCP reset on day 19 inventory in the second run; retry succeeded first attempt. |
| 48 | May 2026 | Bronze notebook is **entity-agnostic** — single notebook ingests any landing entity given an `entity_name` widget. | `notebooks/bronze/ingest_to_bronze.py` reads `landing/{entity_name}/` with `recursiveFileLookup=true` (so `date=*/` and `full/` modes unify), appends 4 standard audit columns (`_source_file`, `_ingestion_timestamp`, `_pipeline_run_id`, `_bronze_timestamp`) plus a partition column `_ingestion_date`, and writes append-only Delta to `bronze.default.{entity_name}` with `mergeSchema=true`. ADF (Tier 6) will eventually invoke this same notebook 10× per pipeline run, one per entity, rather than maintaining 10 near-identical notebooks. | Three Bronze notebook designs were possible: (1) one notebook per entity, hardcoded; (2) one notebook with a switch on entity_name dispatching to entity-specific logic; (3) one entity-agnostic notebook that does the same thing for every entity. Bronze layer's contract is "schema-on-read, append-only, audit columns only — no business logic" (per CLAUDE.md medallion boundaries), which means there *is* no entity-specific logic at the Bronze level. Design (3) makes that contract structurally enforceable — anyone who feels the urge to special-case Bronze for one entity has to fork the notebook, which is a visible architectural deviation. `mergeSchema=true` accommodates schema drift without breaking ingestion (drift is handled at Silver via DQ rules). Smoke-tested with `customers` (158 rows, full mode) — verified row count + audit column population + `_ingestion_date` partition via SQL Warehouse query. |
| 49 | May 2026 | Daily generator runs as a timer-triggered Azure Function with MSI auth and **relative-date resolution** (`MAX(orders.order_date) + 1`), not wall-clock. Bronze + downstream stays out of the Function's scope — that's ADF's job. (**Date-resolution part superseded by #51, 2026-05-06 — switched to wall-clock `today_utc - 1`. Plan-tier part superseded by #50.**) | `PipelineIQ-IaC/core/functions/` provisions a Linux Consumption (Y1) Function App with system-assigned MSI. `generator/main.py::main(mytimer)` resolves the next logical date by querying `velora_oms.orders`, then calls existing `run(run_date)`. `generator/config.py::get_connection_string()` switches to `Authentication=ActiveDirectoryMsi` when `AZURE_SQL_AUTH_MODE=msi` (set as Function App setting). `scripts/grant_function_msi_sql.py` is the one-shot bootstrap that registers the MSI as a SQL principal (`CREATE USER ... FROM EXTERNAL PROVIDER`) and grants role memberships. Deployment via `scripts/deploy_function.sh` — slim ZIP (host.json + requirements.txt + generator/) deployed by `az functionapp deployment source config-zip` with Oryx server-side build. | Three reasons the Function only does the generator: (1) **Architectural fit** — the generator simulates the source-system OLTP, which is a real DB writing rows in real production. ADF (Tier 6) is the proper orchestrator for downstream extract / Bronze / Silver / Gold. Folding Bronze ingestion into a Function would re-invent ADF and create throwaway code (`scripts/export_velora_to_landing.py` and `scripts/run_bronze_smoke.py` are explicitly labeled scaffolding for Phase 2 dev). (2) **Auth boundary** — the Function MSI only needs Azure SQL access; chaining Bronze would require Databricks user provisioning + ADLS contributor + multi-service auth in one process. (3) **Failure radius** — a 3-step Function is one failure point for three logically-separate concerns. ADF gives us per-step retry, watermark management, and diagnostic settings to Log Analytics. **Why relative-date over wall-clock:** Velora's narrative starts on 2026-01-15. If the function used `today()`, day 22 would jump to whatever real-world day the function first runs (likely 2026-05-XX), leaving a 100+ day gap. Relative continuity (max + 1) keeps the demo timeline coherent regardless of when the function fires. Two issues caught + fixed during deployment: (a) `import config` failed in Function context because Azure Functions imports `generator.main` as a package and `generator/` isn't in sys.path — added `sys.path.insert(0, dirname(__file__))` at top of main.py; (b) initial Function App create hit a TCP reset on the management API → had to `terraform import` the resource that was created Azure-side but missing from state. |

| 50 | May 2026 | Function App plan tier: **migrated Y1 Linux Consumption → FC1 Flex Consumption**. Same scale-to-zero economics, reliable timer firing on Linux. | `core/functions/` IaC swapped `azurerm_linux_function_app` for `azurerm_function_app_flex_consumption`, plan SKU Y1 → FC1, added private blob container `app-package-pipelineiq-functions-dev` for the Flex deployment package (auth via account key, simplest path; the runtime storage account is dedicated to this Function App and not shared). MSI principal recreated (`ad0af497-...` → `ccdac37d-5dc5-49b1-8751-3cd19880a2ba`) — required `DROP USER` + re-run `scripts/grant_function_msi_sql.py` to register the new SID against `velora_oms`. `AZURE_SQL_AUTH_MODE=msi` folded into Terraform `app_settings` as drift remediation (was set out-of-band on Y1). | **Y1 + Linux + non-HTTP triggers is the documented sad path.** S5's deploy fired the timer once at 11:19 UTC then went silent for 5 days despite cron `0 0 6 * * *`. Day 22 was the only day produced; days 23–27 all missed. App Insights showed zero telemetry — host wasn't waking. Linux Consumption's ScaleController is unreliable about waking the host for non-HTTP triggers when scaled to zero. Three remediation options considered: (a) Linux Premium EP1 with Always-Ready (~Rs.9,500/mo, overkill for once-daily 10-min run); (b) Y1 + external HTTP warmer ping every 5 min (~Rs.0/mo but a hack — papers over a platform issue with an external dependency, doesn't fix root cause); (c) **Flex Consumption** (~Rs.0/mo under 100K GB-s free grant, Microsoft's documented replacement, reliable timer fires, no external dependency). Chose (c). In-place SKU change Dynamic→FlexConsumption is rejected by Azure (`Cannot update ServerFarm SKU`); used `terraform apply -replace=module.functions.azurerm_service_plan.this`. Verified by manual `admin/functions/generator` invoke after redeploy — day 23 + day 24 landed in velora_oms. Tomorrow 06:00 UTC fire is the timer-reliability proof. |
| 51 | May 2026 | Generator switches to **wall-clock real dates** (`today_utc - 1`) with an **idempotency guard** in `run()`. Supersedes #49's narrative-ordinal `MAX(order_date)+1` date logic. | `generator/main.py::yesterday_utc()` returns `datetime.now(timezone.utc).date() - timedelta(days=1)` and replaces `next_logical_date()`. `run()` gains a 5-line guard at the top: `SELECT COUNT(*) FROM velora_oms.orders WHERE order_date = ?` — if non-zero, log + return `{"_skipped": True, ...}` without writing. Source DB wiped + backfilled fresh: 9 days Apr 27 → May 5 (2026), 3,235 orders / 11,014 lines / 5,930 status logs / 232 customers / 1.7M inventory rows. Catalogue tables kept (idempotent UUID5 per #19, no need to re-seed). | Two motivations. (1) **Demo narrative:** ordinal "day 24 = some Jan 2026 date" gets confusing fast — by day 50 you can't track what day matched what real date. Real wall-clock dates (a) match the demo timestamp the audience sees, (b) match telemetry timestamps in App Insights / Log Analytics, (c) match the canonical "nightly batch from OLTP" enterprise pattern. The Function fires at 06:00 UTC and writes `today-1`, mirroring "Velora's OLTP ran all day yesterday; warehouse picks up that completed day first thing the next morning." (2) **Idempotency:** wall-clock dating exposes a new failure mode — manual backfills overlap with scheduled fires (e.g., user backfills through today; Function fires next day for `today-1` which was already manually seeded → PK collision on identical UUIDs since RNG is date-derived per #41). Guard makes any duplicate fire a no-op rather than a hard error. Rules of operation: manual backfill stops at `today-1` (leave today empty for tomorrow's fire); to genuinely re-seed an already-populated date, wipe its rows from velora_oms first (the guard's "right friction" — re-seeds should be deliberate). PKs are all UUIDs; date columns are not part of any unique constraint, so the only collision risk is RNG-derived UUID overlap, which the guard sidesteps cleanly. Verified twice: local re-run for May 3 logged "already has 274 orders — skipping"; deployed Function manual-invoke skipped May 5 with no DB change. |

---

*Add to this table immediately when any architectural choice is made.
Do not defer — decisions made in session and not recorded here are lost.*
